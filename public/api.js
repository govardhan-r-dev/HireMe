class ApiError extends Error{constructor(message,status){super(message);this.status=status}}
async function api(url,options={}){const controller=new AbortController(),timeout=setTimeout(()=>controller.abort(),30000);try{const headers=options.body instanceof FormData?{...options.headers}:{'Content-Type':'application/json',...options.headers};const response=await fetch(url,{...options,headers,credentials:'same-origin',signal:controller.signal});const data=await response.json().catch(()=>({}));if(!response.ok)throw new ApiError(data.error||'The request failed',response.status);return data}catch(error){if(error.name==='AbortError')throw new ApiError('This request took too long. Please try again.',0);if(error instanceof TypeError)throw new ApiError('Cannot reach HireMe.co. Check your connection and try again.',0);throw error}finally{clearTimeout(timeout)}}
